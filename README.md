## Stash of various things that i need to save somewhere

```
curl -fsSL https://get.docker.com | sudo bash
```

containerd + k8s control plane setup
```
curl -fsSL https://raw.githubusercontent.com/r1zekami/the-stash/refs/heads/main/ctd-k8s-cp.sh | sudo bash
```

k3s
```
curl -fsSL https://raw.githubusercontent.com/r1zekami/the-stash/refs/heads/main/k3s-longhorn-setup | sudo bash
```

```
sudo apt update; sudo apt install -y open-iscsi nfs-common; sudo systemctl enable iscsid; sudo systemctl start iscsid;
```
